import { setupCharacterSection } from './character_section.js';
import { setupHeaderEffects } from './header_section.js';

document.addEventListener('DOMContentLoaded', () => {
    setupCharacterSection();
    setupHeaderEffects();
});
